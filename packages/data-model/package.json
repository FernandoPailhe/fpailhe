{
  "name": "@ferpa/data-model",
  "version": "0.2.0",
  "private": true,
  "description": "Modelo de datos y lógica de dominio pura (tipos, cálculos, formatters). Sin dependencias de React ni UI.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
