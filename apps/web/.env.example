# Base URL del backend de datos.
# Hoy son los JSON estáticos en public/data.
# El día que haya un servidor real, alcanza con apuntar esto a su URL —
# ver apps/web/src/services/dataService.ts.
VITE_API_BASE_URL=/data
